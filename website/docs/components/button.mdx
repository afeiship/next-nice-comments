---
id: button
title: button
sidebar_label: button
slug: /button
---

# 关于

> 一个可以方便写样式的工具。

### 安装

> 直接用 `npm` 或者 `yarn` 安装。

```bash npm2yarn
npm install @jswork/styled-box
```

### 演示

```jsx live
<Button>
  FIXME
</Button>
```
